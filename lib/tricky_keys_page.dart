import 'package:flutter/material.dart';

class TrickyKeysPage extends StatelessWidget {
  const TrickyKeysPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("History"),
      ),
      body: const Text("D"),
    );
  }

}